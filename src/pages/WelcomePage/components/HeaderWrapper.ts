import styled from "styled-components";
import { defaultStyle } from "@/styles/defaultStyle.ts";

export const HeaderWrapper = styled.div`
  border-top: 3px solid ${defaultStyle.colors.SANTANDER_RED};
  margin-bottom: 128px;
`;
