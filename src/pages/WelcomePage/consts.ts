export const BUTTONS_VALUES = Array.from({ length: 9 }, (_, i) =>
  (i + 1).toString(),
);
export const PIN_MIN_LENGTH = 4;
export const PIN_MAX_LENGTH = 7;

export const DEFAULT_PIN = "5173";

export const PIN_PAD_INFO = {
  message: "Enter Your PIN",
  error: "Invalid Pin \n Try Again",
};

export const DEFAULT_LOG_IN_TEXT = "LOGIN";
